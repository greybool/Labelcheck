# BACKLOG — отложенные идеи

Правило: новые идеи попадают сюда, а не в текущий спринт (защита от
скоуп-крипа, см. `docs/TZ-LABELCHECK.md` §4.3 и раздел 9 ТЗ). Пункт берётся
в работу только решением владельца проекта и только из буферных дней.
Формат: что — зачем — откуда возникло.

## Retrieval / токенизация

- **Е-коды с пробелом («Е 635»)** — токенизатор BM25 не склеивает
  пробельную форму с «Е635», такой запрос теряет точность. Возникло при
  анализе реальных упаковок (День 2–3).
- **Слияние part-частей пунктов в матчере** — сейчас части разрезанного
  пункта приложения засчитываются консервативно (строго по chunk_id);
  слияние подняло бы Hit@K на длинных пунктах. Из EVALUATION.md (День 3).

## Аспекты / чек-лист

- **Сверка русских наименований лицевой и обратной сторон** (решение
  Сергея, 2026-08-31): если русское наименование продукции есть и на
  лицевой стороне, и на обратной — сверять их между собой, расхождение
  помечать (кейс mango: «МАНГО кубиками быстрозамороженная» vs
  «БЫСТРОЗАМОРОЖЕННОЕ МАНГО резаное»; кейс краб. палочек: лицевая без
  «имитация»). УТОЧНИТЬ: регламентируется ли идентичность ТР ТС — прямой
  нормы в корпусе не нашли (022 ч.4.2 — про транспортную упаковку);
  ближайшие зацепки — достоверность наименования (022 ч.4.3 п.1) и запрет
  введения потребителя в заблуждение. Реализация: правило в check-текст
  аспекта 1 (сверка по фактам, регионы product_name) — дёшево; либо
  детерминированная сверка кодом при нескольких product_name-регионах.

- **Аспект «изображения на упаковке»** (022 ч.4.12 п.8–9): нельзя изображать
  продукты, которых нет в составе; изображение готового блюда — только со
  словами «вариант приготовленного блюда». Отложено Днём 5: требует
  распознавания СОДЕРЖАНИЯ картинок (не текста) — новый класс vision-задач
  с риском ложных срабатываний. (День 5.)
- **Замер высоты шрифта в мм по PDF-макету** — физические размеры страницы
  макета известны, высоту литер можно мерить по рендеру; закрыл бы аспект 17
  (2 мм / 0,8 мм из 022 ч.4.12 п.1) без ручной проверки. (День 5.)
- **Категория children (детское питание)** — 034 п.126, 040 п.75, детские
  нормы прил.2 к 022: отдельная ветка category_regulations, если в
  ассортименте появится детская заморозка. (День 5.)
- **Запрос аспекта 21 разбить на отдельные клеймы** — CHEAP-модель при
  переформулировке склеила примеры («низкокалорийный источник белка без
  сахара») в одну фразу; безвредно (оригинал всегда в слиянии), но запросы
  точнее по одному клейму. (День 5, смоук rewriting.)
- **EN/KK-маркеры категорий продукта** — словари category_markers почти
  целиком русские (латиницей только «chicken»); если русский блок макета
  не прочитан или отсутствует, категория по EN/KK-тексту не определится
  (salmon, shrimp, beef, 닭고기 и т.п.). Подстраховка есть — кнопки
  --categories, но словари стоит расширить. (Сессия 5, финальная ревизия.)
- **Стабильность вердиктов между прогонами** — на трёх e2e-прогонах одного
  макета пограничные аспекты (6, 15, 16) меняли статус: суждения на границе
  «соответствует/ручная» недетерминированы. Варианты: self-consistency
  (N прогонов + голосование), понижение temperature, кэш вердиктов по
  SHA256(макет+конфиг+промпт). Меряет День 8. (День 6, e2e.)
- **Кэш вердиктов** — повторная проверка того же макета стоит те же ~$0.5;
  ключ — SHA256 макета + конфига; пригодится UI (День 9) и evaluation
  (День 8). (День 6.)

- ✅ СДЕЛАНО (День 9): **Прил.1 к 022 в basis аспекта «Состав» + правило вида мясного сырья**
  (доменное ревью v4 + сверка с внешним ответом, 2026-08-30): п.4 ч.4.4
  022 отсылает к прил.1 (перечень допустимых обобщённых имён компонентов:
  «рыба» там есть, мяса/птицы нет), но само прил.1 (2 чанка, ~2 тыс.
  симв.) в контекст вердикта не передаётся — цепочка ссылок рвётся, и
  правило 5 промпта честно блокирует вывод «от отсутствия». Сделать:
  прил.1 в basis аспекта 2 + в check-текст правило «мясной компонент
  назван достоверно (вид сырья); просто "курица" → рекомендация запросить
  вид сырья у производителя, исключить/подтвердить мехобвалку (051
  п.106)». Решение Сергея 2026-08-30: доделать (после Дня 7).

- ✅ СДЕЛАНО (День 9, с поправкой): **029: разрешённость добавок по категориям продукции** (доменное ревью
  v4): вердикт по добавкам не может однозначно подтвердить разрешённость
  добавки для категории продукции — гигиенические нормативы по категориям
  лежат в табличных приложениях 029 (прил.19 — 655 окон, прил.26 — 73,
  прил.12 — 70), куда basis и Е-код-lookup не ходят, а ретрив попадает
  случайно. ПОПРАВКА Дня 9 по данным корпуса: прил.19 — ароматизаторы
  (CAS-номера, Е-кодов 0 из 655 окон), прил.26 — ферменты, прил.12 —
  носители; реальные нормативы по категориям — прил.3–18 («добавка |
  пищевая продукция | максимальный уровень», Е-коды есть). Реализован
  hygiene_lookup по прил.3–18 (config → verdict.ecode_lookup); маппинг
  «продукт → классификатор 029» кодом не решался — оставлен модели и
  проверке Сергея. Сделать: второй шаг lookup'а — категория продукции
  (классификатор 029 + кнопки категорий) → детерминированно ВСЕ окна
  нормативов с данным Е-кодом → вердикт цитирует строку («разрешён для
  категории, предел…» / «в перечне категории отсутствует — возможное
  нарушение»). ~день работы; риски: окна режут строки таблиц (перехлёст
  3 строки), маппинг «продукт → категория 029» требует доменной сверки
  Сергея. Кандидат на буферные дни 12–14.

- **Агент с инструментами поиска по регламентам (REVIEW-LOG R-09, отложено
  решением Сергея 02.09).** Идея: дать вердикт-модели инструменты
  `search(query, regulations)` / `get_clause(chunk_id)`, чтобы она сама
  дозапрашивала пункты, когда переданного контекста не хватает. Оценка:
  ~день на цикл tool-use + день на тесты и замеры стабильности (агентные
  циклы менее стабильны, стабильность 95% достигнута голосованием);
  стоимость +$0,03–0,05 за каждый доп. шаг MAIN-модели, при 1–2 шагах на
  аспект — +$1–2 на макет (сейчас ~$0,9) и в 1,5–2 раза дольше. По отчётам
  реальная нужда — два места: аспект 21 (условия прил. 5 к 022 + суточные
  нормы прил. 2) и аспект 4 (нормативы по категориям); оба закрываются
  целевыми lookup'ами кодом, как Е-код-lookup пункта B. Сначала — R-19
  (арифметика клеймов кодом), агент — если после этого останутся случаи.

## Vision-модуль

- **Макеты БЕЗ текстового слоя: дробить крупные регионы** (2026-08-31,
  hold-out «Волшебные макеты 1» — крабовые палочки): обзор дал всего
  3 региона на весь макет; тексты прочитаны полностью, но гранулярность
  грубая, а все сторожа слоя (layer_fill, unread, «выдумки», опечатки)
  на таких макетах слепы. Идея: при регионе крупнее N% площади —
  повторный обзор кропа региона (рекурсивное дробление); плюс пометка
  в отчёте «текстового слоя нет — типографика не проверялась».

- **Просмотрщик макетов (review.html) — доработки** (Сергей, 2026-08-30):
  зум и подсветка спорных сделаны; остальные замечания по отображению
  Сергей озвучит позже; целевое место инструмента — вкладка Streamlit
  (рамки + правка текста человеком ДО вердиктов + заметки «на перепрогон» /
  «замечание дизайнеру»).
- **Перечитка мелких растровых бейджей увеличенным кропом**: эмблемы вроде
  짭짤고소 / 육즙가득 vision не транскрибирует или даёт мусор; unread-сторож
  их видит — добавить второй проход: кроп бейджа с 2–3-кратным увеличением.
- **Недетерминизм обзора между прогонами**: рамки/гранулярность меняются от
  прогона к прогону; принятая схема — канонический layout фиксируется
  (data/layouts, v4 от 2026-08-30), перечитка только осознанно; страховки —
  сторожа (чернила, слой в обе стороны, хангыль) + правка человеком в UI.
- **Судья (LLM-eval): голосование большинством из 3 вызовов + сообщить
  промпту судьи принцип «неуверен → ручная проверка — это НЕ ошибка»**
  (замер Дня 8: самосогласованность строгого судьи 6/12) — поднять, если
  останется время после основных дней.


- **Кросс-языковая сверка составов RU/EN/KK** — на реальных макетах составы
  на разных языках расходятся (добавки, присутствующие только в одном
  языке) — это потенциальные нарушения, которые сейчас не ловятся. Плюс
  замечено: на языках вне скоупа модель может тихо заменить термин
  смысловым синонимом — сверка должна учитывать это. (Дни 1 и 4.)
- **Кир/лат микс внутри слова в текстовом слое** («hалейте» с латинской h) —
  ЧАСТИЧНО СДЕЛАНО после сверки Дня 6: слова слоя, не найденные в
  vision-тексте, выводятся в отчёт как кандидаты-опечатки, гомоглифы
  помечаются отдельно. Осталось: нормализация при сшивке для coverage
  (ложное «непрочитано»). (День 4 → День 6.)
- **Многостраничные макеты** — читается только первая страница
  (`page_index=0`); `page_count()` уже есть. Нужен цикл по страницам
  и объединение JSON. (День 4.)
- **Retry/backoff на ошибки API** — vision.py падает исключением на сетевой
  ошибке или rate-limit; для батч-прогонов (evaluation Дня 8) нужен
  повтор с паузой. (День 4.)
- **Кэш vision-результатов по SHA256 PDF** — повторный прогон того же
  макета стоит те же $0.06–0.09; пломба source_sha256 уже в meta,
  осталось смотреть в data/layouts/ перед вызовами. Пригодится UI (День 9)
  и evaluation (День 8). (День 4.)
- **Responses API вместо chat.completions** — рекомендации OpenAI для
  документов (`text.verbosity=high` для дословности, `detail=original`)
  доступны только там; сейчас читаем через chat.completions с detail=high.
  Оценить, даёт ли переход прирост качества чтения. (День 4.)
- **Детерминизация обзора (seed)** — число регионов и kind-метки гуляют
  между прогонами; параметр seed мог бы стабилизировать прогоны для
  сравнения конфигураций. (День 4.)
- **Порог-предупреждение для coverage** — сейчас доля покрытия слоя только
  сохраняется и печатается; ниже порога (например 90%) можно поднимать
  явный флаг неполноты в JSON. (День 4.)
- **Текстовый слой как черновик чтению** — отвергнуто из-за риска якорения
  на невидимый/устаревший текст под кривыми; вернуться, только если
  качество чтения макетов в кривых просядет. (День 4, решение владельца.)
- **kind_markers для product_name** — полнота названия продукта сейчас
  проверяется только kind-меткой (сигнальных слов у названия нет);
  подумать над эвристикой (крупнейший кегль на лицевой панели). (День 4.)

## Проверки без LLM

- **Арифметика энергетической ценности** — СДЕЛАНО после сверки Дня 6
  (решение Сергея): расчёт ккал/кДж по БЖУ (прил.4 к 022) идёт в промпт
  аспекта 6 и в отчёт. Спирт и полиолы в расчёте не учитываются —
  расширить при появлении таких продуктов. (День 1 → День 6.)
- **Контрольная цифра EAN-13** — штрихкод уже читается vision-модулем
  с цифрами; валидация контрольной цифры — чистая арифметика. (День 1.)
- **Кросс-языковая сверка** — тест-кейсы с реального макета (сверка Дня 6):
  «пшеница/wheat» есть в RU/EN, отсутствует в KO; два сиропа в RU/EN против
  одного 물엿 в KO; 유화제 без Е-кода. Буфер Дней 12–14, приоритет по ТЗ.

## Документы / README

- **Правки ТЗ одним заходом**: дедлайн «= 8 сентября 06:00 по Ханою»;
  убрать «решение Сергея» у 034/040; verify_corpus.py вместо download.py
  в разделе 10; оценка эмбеддингов $0.01 → факт $0.024; PIPELINE.md в
  список docs; «scale ~6» в описании Дня 4 → фактический scale 4;
  §4.4 — аспектов стало 21 (20 «Предупредительные надписи»,
  21 «Отличительные признаки», решение Дня 5); §4.2 — категорийные
  регламенты (034/040) подтягиваются авто-детектом по словам-маркерам
  наименования и состава (решение Дня 5); §4.5 — уточнить формулировку
  «никакого классификатора типов»: словарная эвристика по маркерам
  (уровень kind_markers) классификатором не считается; §4.4 аспект 17 —
  основание ч.4.12 п.1 с конкретными размерами шрифта (2 мм / 0,8 мм),
  а не «соответствие установленным размерам» без адреса; раздел 3 (корпус) —
  СЕМЬ регламентов: добавлен ТР ЕАЭС 051/2021 (мясо птицы, решение Сергея
  после сверки Дня 6, 034 п.4в исключает птицу), категории продукта —
  три кнопки poultry/meat/fish.
- **README: секция-обоснование vision-схемы** — эксперимент Дня 4
  («весь макет одним вызовом» → выдуманные ингредиенты-аллергены на
  ужатом изображении) — сильная история для ревьюеров о том, почему
  выбрана двухпроходная схема.
- **README: Python 3.12 в шаге установки** — requirements (numpy 2.4)
  требует ≥3.11. (Из handoff-03.)
- **Список внешних отсылок регламентов (ГОСТы, решения)** — так и не
  собран; не блокирует. Дополнение Дня 5: Решение КТС № 711 (изображение
  и правила нанесения знака ЕАС) сознательно вне корпуса — аспект 14
  опирается на статьи о знаке в самих ТР.

## Уточнить у Сергея (отложено 2026-08-31)

- **Замечания по просмотрщику** review_vision_v3.html — обещал озвучить,
  отложено «на позже»; спросить перед работой над UI (День 9), чтобы учесть.
- **Заметки из просмотрщика** — накопились ли; выгрузка кнопкой в
  vision_review_notes.json, попросить приложить/показать путь.
