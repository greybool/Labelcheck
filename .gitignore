# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# Environment variables / secrets
.env
.env.*
# ...но шаблон .env.example — публичная часть репозитория
!.env.example

# IDE
.vscode/
.idea/

# Jupyter
.ipynb_checkpoints/

# Logs
*.log

samples_private/

handoffs/

# Аудит парсера — не для репозитория
data/dropped_lines.txt

# Кэш эмбеддингов — пересоздаётся командой: python ingestion/index.py
data/embeddings.npz

# Vision-выводы макетов (полные тексты реальных этикеток) — не для репозитория
data/layouts/

# Отчёты проверки (тексты и вердикты реальных макетов) — не для репозитория
data/reports/

# Кэш ответов вердикт-модели (в промптах — тексты реальных макетов)
data/verdict_cache.json

# Кэши оценки Дня 7 — пересоздаваемые (evaluation/runs/ и metrics/ — в git)
data/gt_embeddings.npz
evaluation/ground_truth.partial.jsonl
# Эталонная разметка vision-оценки — тексты реальных макетов, не в git
data/vision_gt/

# Прогоны vision новой схемы (приёмка) — тексты макетов, не в git
data/layouts_v2/
data/layouts_v3/
