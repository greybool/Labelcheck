# REVIEW-LOG — журнал замечаний приёмки

Живой документ: каждое замечание владельца системы (Сергей) получает номер,
разбор по сырым данным, решение и статус. Замечание закрывается только
после подтверждения Сергеем на живом прогоне. Макеты названы по продукту
(бренды и имена файлов — не в репозитории).

Статусы: `открыто` → `в работе` → `сделано` → `подтверждено` · `бэклог`
(не в этом спринте, причина указана) · `отклонено` (с обоснованием).

Приоритеты: **P1** — ломает работу или искажает результат, делать первым;
**P2** — качество вердиктов/отчёта, дёшево (промпт, конфиг, код без LLM);
**P3** — зрение, требует прогонов с API; **P4** — новая функциональность.

## Сводная таблица

| ID | Источник | Тип | Суть | P | Статус |
|---|---|---|---|---|---|
| R-01 | общее | vision | Обрезает блоки при чтении | P3 | открыто |
| R-02 | общее | vision | Неверный тип блока (kind) | P3 | открыто |
| R-03 | общее | UI-баг | «Следующий блок» возвращает на прежний | P1 | подтверждено 02.09 |
| R-04 | общее | UI-баг | Переход шаг 2→1→2 теряет оценки и решения | P1 | подтверждено 02.09 |
| R-05 | общее | продукт | Убрать автодетект профильных ТР; базовые по умолчанию + опции | P1 | подтверждено 02.09 |
| R-06 | общее | план работ | Ужимание замечаний в плане (теряются пункты) | P1 | шаг A подтверждено 02.09; шаг B открыт (P4) |
| R-07 | общее | vision | Систематически не видит петлю Мёбиуса | P3 | открыто |
| R-08 | общее | UI | Нельзя подтвердить «сомнительный» блок без правки текста | P1 | подтверждено 02.09 |
| R-09 | общее | идея | Агент с инструментами для доп. обращений к регламентам | P4 | бэклог |
| R-10 | Roti | vision/отчёт | Ложное «не найден обязательный блок: название» | P2 | сделано 03.09, ждёт подтверждения |
| R-11 | Roti | категории | Ложный автодетект «мясная продукция» | P1 | закрыто R-05 |
| R-12 | Gyoza | UI | Показывать все расхождения со слоем, два столбца | P3 | сделано 03.09, ждёт подтверждения |
| R-13 | Gyoza | орфография | 49 «неточностей» — раздутый пропуск одного блока | P2 | сделано 03.09, ждёт подтверждения |
| R-14 | Gyoza | вердикты | Иноязычные слова состава без перевода в выводах | P2 | сделано 03.09, ждёт подтверждения |
| R-15 | Gyoza | vision/отчёт | «молодой»→«молотый»: молчаливое исправление опечатки макета | P2 | сделано 03.09, ждёт подтверждения |
| R-16 | Mandu | штрихкод | Выдуманный код 4820140240955 не пойман сторожем | P2 | сделано 03.09, ждёт подтверждения |
| R-17 | Mandu | орфография | 23 «неточности» — та же природа, что R-13 | P2 | сделано 03.09, ждёт подтверждения (через R-13) |
| R-18 | Крабовые палочки | вердикт 20 | Объяснение смешивает две разные надписи, читается как противоречие | P2 | сделано 03.09, ждёт подтверждения |
| R-19 | Крабовые палочки | вердикт 21 | «Источник белка»: проверено одно условие из двух, расчёт не показан | P4 | открыто |
| R-20 | Крабовые палочки | орфография | «Разнобой» — непонятная формулировка, нет действия | P2 | сделано 03.09, ждёт подтверждения |
| R-21 | Креветки | вердикт 7 | Не распознала «Изготовлено и упаковано:» как поле даты | P2 | сделано 03.09, ждёт подтверждения |
| R-22 | Креветки | вердикт 1 | Отказ от вывода из-за ненадёжных блоков | P2 | сделано 03.09, ждёт подтверждения |
| R-23 | Креветки (найдено при разборе) | vision | Сторож «выдумок» массово ложно срабатывает на PDF, где часть текста в кривых | P3 | сделано 03.09, ждёт подтверждения |
| R-24 | Gyoza (найдено при разборе) | vision | Качество чтения плавает между прогонами одного PDF; нет автоповтора при низком покрытии | P3 | предупреждение сделано 03.09; автоповтор открыт |
| R-25 | общее | орфография | Находки аспекта 19 меняются от прогона к прогону | P2 | голосование подтверждено данными 04.09; добавлен фильтр кодом, ждёт прогона |
| R-26 | Креветки | vision | Текст по дуге («вариант сервировки продукта») не прочитан | P3 | промпт дополнен 04.09; проверить прогоном |
| R-27 | общее | вердикты | «Регионы t1r5, t1r7 прочитаны ненадёжно» при надёжных блоках | P1 | подтверждено 04.09 |
| R-28 | Креветки | орфография | «X → Y» — непонятно, какой вариант правильный | P2 | подтверждено 04.09 |
| R-29 | Gyoza | UI-баг | Снова «следующий» откатывает после «Сохранить» (регрессия R-03) | P1 | подтверждено 04.09 |
| R-30 | Gyoza | UI | Сверка со слоем: раскрыть сразу, переименовать колонки | P2 | подтверждено 04.09 |
| R-31 | Gyoza | вердикт 20 | Е420: надпись о подсластителе требуется без учёта порога 10% | P2 | закрыто решением 04.09: предупреждать всегда |
| R-32 | Gyoza | вердикт 2 | Лишние рассуждения («свинина… по птице проблемы нет») | P2 | сделано 04.09, ждёт подтверждения |
| R-33 | Gyoza | вердикт 12 | ГМО: ручная проверка без причины и без конкретики | P2 | сделано 04.09, ждёт подтверждения |
| R-34 | Gyoza | орфография | «Молодой перец» не найден орфографией | P2 | сделано 04.09, ждёт подтверждения (промпт); проверить прогоном |
| R-35 | Roti | UI | Крупный вид блока обрезан по краям | P2 | подтверждено 04.09 |
| R-36 | Mandu | vision | Блок t1r2 (лицевая панель с логотипом) прочитан плохо | P3 | открыто |
| R-37 | Mandu | vision | «Знак EAC» приписан регионам без знака (4 региона) | P2 | промпт + правило аспекта 14 сделаны 04.09; проверить прогоном |
| R-38 | Mandu | штрихкод | Код прочитан, а аспект 18 пишет «не найден» | P1 | подтверждено 04.09 |
| R-39 | Mandu | вердикт 21 | «Источник протеина»: одно условие из двух (= R-19) | P4 | бэклог P4 (решение 04.09, = R-19) |
| R-40 | общее | UI/данные | Два layout'а одного PDF в списке; канонический не пересчитан | P2 | сделано 04.09, ждёт подтверждения |
| R-41 | Gyoza (скриншот 04.09) | UI/данные | «Не найдены обязательные блоки: product_name» после R-10 | P2 | сделано 04.09, ждёт подтверждения |

---

## Общие замечания

### R-01 · Обрезает блоки при чтении — P3
**Замечание.** Vision обрезает блоки.
**Данные.** Gyoza, блок t1r12 (весь оборотный текст): канонический прогон
(31.08) — 6426 символов, покрытие слоя 98%; прогон из UI (02.09) — 5083
символа, покрытие 82%. Пропал целый раздел «Способ приготовления: на плите /
в микроволновке / в пароварке» — остался только «во фритюре». Рамка та же,
потеряно чтение, не обзор. Mandu, блок t2r2: текст обрывается на «в большой
каст… наприк» — кроп прочитан не до конца.
**Гипотеза.** Кроп длиннее 2000 px режется пополам с перехлёстом — модель
чтения на одной из половин «сжала» длинный текст, либо склейка половин
теряет середину. Требует воспроизведения на конкретном кропе.
**Предложение.** (1) Воспроизвести на t1r12: сохранить обе половины кропа
и ответы модели, найти, где именно теряется текст. (2) Сторож по региону:
если доля слов слоя внутри рамки, не найденных в чтении, > порога —
перечитать регион меньшими кусками (сейчас повтор идёт только при пустом
чтении). Связано с R-24.
**Статус.** открыто.

### R-02 · Неверный тип блока (kind) — P3
**Замечание.** Местами неправильно определяет тип блока.
**Данные.** Roti: r1 «Лепешка Roti. Полуфабрикат хлебобулочный…» →
`other_text` (должен быть `product_name`); r7 условия хранения + изготовитель
→ `net_weight`; r8 адрес и импортёр → `dates_storage`. Gyoza: весь оборот
одним блоком `other_text`.
**Влияние.** На вердикты почти не влияет — каждому аспекту передаётся весь
макет, kind не фильтр. Влияет на: список `missing` (R-10), прицельный
автодетект категорий (R-11), подписи блоков в интерфейсе.
**Предложение.** Не бороться с недетерминизмом меток обзора, а снять
зависимость от них: R-10 и R-05 убирают два из трёх потребителей kind.
Подпись в UI — оставить как ориентир.
**Статус.** открыто.

### R-03 · «Следующий блок» возвращает на прежний — P1
**Замечание.** На шаге 1: правлю текст → «Сохранить» → «следующий» —
перекидывает не на следующий, а на какой-то прежний блок.
**Данные / причина.** Компонент клика по картинке (`layout_click`) хранит
ПОСЛЕДНИЙ клик и отдаёт его при каждой перерисовке. Код: `if rid and rid !=
ss.sel_region: ss.sel_region = rid; st.rerun()`. После «следующий» блок
меняется на Y, страница перерисовывается, компонент снова отдаёт старый
клик по X → `X != Y` → выбор откатывается на X. Проявляется только после
хотя бы одного клика мышью по макету; «Сохранить» ни при чём — любая
перерисовка.
**Решение.** Запоминать обработанный клик (`ss.last_click`) и реагировать
только на новый. Стандартный паттерн для этого компонента.
**Сделано 02.09.** `app.py`: подпись клика (x, y, width, height) сравнивается
с последней обработанной. Проверить руками: клик по блоку → «следующий» →
должен открыться следующий, а не кликнутый.
**Статус.** подтверждено 02.09.

### R-04 · Переход шаг 2 → 1 → 2 теряет оценки и решения — P1 (обязательно)
**Замечание.** Ставлю оценки и решения на шаге 2, ухожу на шаг 1 посмотреть
блок, возвращаюсь — всё потеряно.
**Данные / причина.** Виджеты решений имеют ключи `dec_N`, `rate_N`,
`note_N`. Streamlit удаляет из `session_state` значения виджетов, которые не
отрисовались в текущем прогоне — на шаге 1 они не рисуются, ключи стираются.
При возврате `ss.get("dec_N", default)` даёт значение по умолчанию.
**При этом данные НЕ пропали**: автосохранение писало их в `labelcheck.db`
(таблица `feedback`, `upsert_feedback`). Потерян только показ. Тот же
эффект бьёт по шагу 3: план работ читает решения из тех же ключей — после
захода на шаг 1 план строится как будто решений не было.
**Решение.** При отрисовке шага 2 и шага 3 — если ключа нет в
`session_state`, поднять значение из БД по `check_id`. Побочная польза:
оценки переживут перезапуск приложения.
**Сделано 02.09.** `store.decisions_for_check` + `app.restore_decisions`,
вызывается перед отрисовкой шагов 2 и 3; значения по умолчанию виджетов
теперь тоже ставятся через session_state (один путь). Тест
`test_decisions_survive_step_switch` воспроизводит сценарий: шаг 2 → 1 → 2.
**Статус.** подтверждено 02.09.

### R-05 · Убрать автодетект профильных регламентов — P1 (решение Сергея)
**Замечание.** По упаковке однозначно не определить. По умолчанию — проверка
по базовым регламентам (перечислить, по каким), плюс опция добавить мясо /
птицу / рыбу-морепродукты.
**Данные.** Roti: автодетект дал «мясная продукция» по слову «мясом» во
фразе «Подавайте с яйцом, мясом, овощами» (R-11). Ранее: «мясо», «фарш»
тянули и мясо, и птицу на пельменях.
**Контраргумент (озвучен).** На крабовых палочках и манду автодетект
попадал верно и экономил клик. Компромисс: маркеры не подключают регламент,
а показывают подсказку «в тексте найдены слова: краб, минтай — возможно,
рыба и морепродукты?» рядом с переключателями. Решение за Сергеем.
**Решение.** Режим «определить автоматически» убрать. По умолчанию — только
базовые (022, 021, 029, 005 — список уже показан на шаге 2 и остаётся),
три переключателя профильных всегда видны. **Решение Сергея 02.09:
автодетект убрать полностью, без подсказки** — по упаковке вид продукции
однозначно не определяется, решает человек. Словари маркеров остаются в
конфиге (нужны тестам и CLI-флагу `--categories auto`), но интерфейс их
не вызывает. `category_scan` в журнале станет `manual` / `none`.
**Сделано 02.09.** Шаг 2: два блока — «Базовые регламенты — применяются
всегда» (список) и «Профильные — подключите, если продукт к ним относится»
(три переключателя, по умолчанию выключены, подпись объясняет, что для
овощей/фруктов/ягод так и нужно). Переключателя режимов больше нет.
**Статус.** подтверждено 02.09.

### R-06 · Ужимание замечаний в плане работ — P1
**Замечание.** Орфография с десятками пунктов при выборе «проверить самому»
на шаге 3 сжимается, большая часть пропадает. Проверить ужимание по всем
пунктам: замечания нельзя терять, только уплотнять. Система должна уметь
внутри одного аспекта выделять отдельные пункты замечаний.
**Данные / причина.** Три места потери:
1. `app.py`, шаг 3, прочие замечания: `"; ".join(block["items"][:2])` —
   в план идут **первые два** пункта из N.
2. `actions.py`: правило промпта «одно замечание — один пункт» плюс
   дедуп `aid in seen` — **один пункт на аспект**; если в объяснении
   несколько находок, остаётся одна.
3. `actions.py`: в модель уходит `explanation[:900]` — длинное объяснение
   обрезано на входе.
**Решение (два шага).** Шаг A (сейчас): прочие замечания переносить в план
1:1 (это уже списки); в `actions.py` снять ограничение «один пункт на
аспект» и обрезку входа — модель возвращает столько пунктов, сколько
находок. Шаг B (структура ответа аспекта — `findings[]` вместо сплошного
текста, с валидацией каждого пункта) — правка промпта вердикта, схемы,
валидатора и интерфейса; ценно, но крупно — после сдачи или если останется
время. Связано с R-18.
**Сделано 02.09 (шаг A).** Прочие замечания идут в план каждым пунктом
(👎 по блоку убирает их, своя формулировка заменяет одним); в `actions.py`
снят дедуп «один пункт на аспект» (режутся только дословные повторы),
объяснение уходит в модель целиком, промпт просит «одна находка — один
пункт». Своя формулировка человека заменяет все пункты аспекта одним.
**Статус.** шаг A подтверждено 02.09; шаг B — открыто (P4).

### R-07 · Не видит петлю Мёбиуса — P3
**Замечание.** Систематически не видит петлю Мёбиуса.
**Данные.** Roti (Сергей подтверждает знак на упаковке): vision описал
только «знак EAC», «знак “для пищевой продукции”». Аспект 15 честно ушёл в
«требует ручной проверки» (3 голоса из 3) — не нарушение, но и не ответ.
На Gyoza и крабовых палочках знак описан («знак переработки пластика 04
PE-LD», «C/LDPE») — там рядом текстовый код материала. Гипотеза: обзор
ищет ТЕКСТОВЫЕ блоки; пиктограмма без текста рядом не попадает ни в одну
рамку, а промпт чтения не называет знаки по именам.
**Предложение.** (1) В промпт обзора: знаки без текста — отдельные регионы
`marks`. (2) В промпт чтения — словарь: «петля Мёбиуса (треугольник из трёх
стрелок, внутри/под ним код материала)», «бокал-вилка», «EAC», «снежинка».
(3) Проверить глазами на рендере Roti, где знак и в какую рамку он
(не) попал — до правки промптов. Требует прогонов зрения ($0.10/макет).
**Статус.** открыто.

### R-08 · Подтвердить «сомнительный» блок без правки — P1
**Замечание.** Блок помечен сомнительным, ошибок в нём нет, а перевести в
«проверенный» нельзя; шаг 2 потом предупреждает «блоки не проверены».
**Данные.** Статус меняется только через `apply_region_edit` (сохранение
изменённого текста); кнопка «Сохранить» заблокирована, если текст не менялся.
**Решение.** Кнопка «✅ Текст верный — подтвердить» рядом с «Сохранить»:
статус → «прочитано», причина → «подтверждено человеком», запись в
историю `edits` (тип `confirm`). Учитывается в метрике качества
распознавания дашборда как «человек проверил, правок не потребовалось».
**Сделано 02.09.** `store.confirm_region` + кнопка «✅ Текст верный —
подтвердить» рядом с «Сохранить» (активна только у сомнительных блоков без
несохранённых правок). Флаг региона `human_confirmed`.
**Статус.** подтверждено 02.09.

### R-09 · Агент с инструментами — P4 (оценка)
**Замечание.** Дать модели возможность обращаться к регламентам
дополнительно, если в переданных пунктах нет нужной информации. Насколько
быстро реализуемо и сколько стоят доп. запросы.
**Оценка.** Реализуемость: цикл tool-use с двумя инструментами
(`search(query, regulations)`, `get_clause(chunk_id)`) — примерно день
работы плюс день на тесты и замеры стабильности (агентные циклы менее
стабильны, а стабильность мы поднимали до 95% голосованием). Стоимость:
каждый дополнительный шаг — ещё один вызов MAIN-модели с полным контекстом,
ориентир $0,03–0,05; при 1–2 доп. шагах на аспект прогон дорожает на
$1–2 (сейчас ~$0,9) и удлиняется в полтора-два раза.
**Где реально нужен.** По отчётам — в двух местах: аспект 21 (условия
прил. 5 к 022 + суточные нормы прил. 2) и аспект 4 (нормативы по
категориям). Оба закрываются целевыми lookup'ами кодом — как уже сделано для
Е-кодов (пункт B). Это дешевле, детерминированно и проверяемо тестами.
**Рекомендация.** До сдачи — не делать; в бэклог с этой оценкой. Сначала
R-19 (арифметика клеймов кодом) — покрывает главный случай.
**Статус.** бэклог (решение Сергея 02.09, запись в BACKLOG.md).

## По макетам

### R-10 · Roti: ложное «не найден обязательный блок: название» — P2
**Данные.** `missing: ['product_name']`, хотя название прочитано (r1
«Лепешка Roti…», kind `other_text`). То же на Gyoza. Причина: для всех
обязательных типов есть сигнальные слова (`kind_markers`: «состав:»,
«масса нетто»…), а для названия сигнального слова быть не может — проверка
опирается только на метку обзора, которая недетерминирована (R-02).
**Решение.** Убрать `product_name` из `required_kinds`: предупреждение
не несёт информации, а наименование проверяет аспект 1 по всему тексту.
**Сделано 03.09.** `product_name` убран из `required_kinds` (config.yaml, комментарий с причиной); наименование проверяет аспект 1 по всему тексту.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-11 · Roti: ложный автодетект «мясная продукция» — P1
**Данные.** Маркер `мяс` сработал на «Подавайте с яйцом, мясом, овощами
или соусами» — рекомендация по подаче, охват `targeted` (kind-метки не
отделили состав от рекомендаций, см. R-02).
**Решение.** Закрывается R-05.
**Статус.** закрыто R-05 (02.09).

### R-12 · Gyoza: все расхождения со слоем, два столбца — P3
**Замечание.** В статусе блока — «расхождение с текстовым слоем 21%: 6분간,
7분간, hалейте, plaease, большой, варить, вкусу, воду» — показаны 8 слов из
~50. Нужен полный список в два столбца: как в текстовом слое / как прочитано.
**Данные.** В layout по региону хранятся `invented_words` (vision-слова не из
слоя, до 12) и общий по странице `unread_layer_words` (50); порегионного
списка непрочитанных слов слоя нет, причина статуса обрезана до 8 слов.
**Решение.** Хранить в регионе полный `layer_missing_words`; на шаге 1 —
таблица: «в слое / прочитано» (пары подмен — R-15), «в слое, не прочитано»,
«прочитано, в слое нет». Данные есть, нужна структура + вывод.
**Сделано 03.09.** В регионе layout'а теперь полные списки: `layer_missing_words` (слой, не прочитано, до 200), `invented_words` (прочитано, в слое нет, до 100), `invented_digits`, `word_substitutions` (пары R-15). Шаг 1: под текстом блока — раскрывающаяся «Сверка с текстовым слоем PDF — расхождений: N»: таблица подмен, два столбца «в слое есть, не прочитано» / «прочитано, в слое нет»; для блока частично в кривых — пометка, что «в слое нет» — скорее кривые, чем выдумка. Для уже распознанных макетов поля заполняет `python -m labelcheck.vision <pdf> --reguard <layout.json>` (без API).
**Статус.** сделано 03.09, ждёт подтверждения.

### R-13 · Gyoza: 49 «неточностей» орфографии — P2
**Данные.** 45 из 49 — строки вида «слово слоя «варить» не найдено в
прочитанном тексте» по одной на слово. Все слова — из одного непрочитанного
раздела «Способ приготовления» (R-01). Это не 45 дефектов макета, а один
пропуск чтения, показанный 45 раз — и он маскируется под орфографию.
**Решение.** Группировать: (а) гомоглифы — каждое отдельно (это дефекты
макета); (б) пары подмен — отдельно (R-15); (в) остальное — одной строкой:
«N слов текстового слоя не найдены в прочитанном тексте (первые: …) —
вероятно, не прочитан блок; проверьте распознавание на шаге 1». Плюс
низкое покрытие слоя выводить как предупреждение о распознавании на шаге 1,
а не как орфографию.
**Сделано 03.09.** `verdict.layer_word_findings` группирует: (а) подмены слов из регионов (R-15) — каждая отдельно, первыми; (б) гомоглифы — каждый отдельно; (в) остальные непрочитанные слова слоя — ОДНОЙ строкой с числом и восемью примерами; при покрытии ниже `coverage_warning_threshold` (0.9) строка называет это пропуском распознавания и отсылает к шагу 1. На шаге 1 при низком покрытии — раскрывающийся список всех непрочитанных слов (часть R-24). Слова из пар в общий список не дублируются.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-14 · Gyoza: иноязычные слова состава без перевода — P2
**Замечание.** В выводах — корейские и английские слова из состава без
русского эквивалента; проверяющий не может сопоставить.
**Решение.** Правило в системный промпт вердикта: слово из иноязычного
блока упоминается только с русским эквивалентом в скобках и с указанием,
из какого блока оно взято.
**Сделано 03.09.** Правило 6 системного промпта вердикта: слово из иноязычного блока — только с русским переводом в скобках и указанием региона.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-15 · Gyoza: «молодой» → «молотый» — молчаливое исправление — P2 (принцип)
**Замечание.** На макете «молодой черный перец» (ошибка макета), система
прочитала «молотый» как будто ошибки нет. Додумывать и молча исправлять
нельзя — это именно те ошибки, которые надо ловить все.
**Данные.** Канонический прогон Gyoza (31.08) прочитал «молодой» верно; UI-
прогон (02.09) — «молотый». Сторож слоя **сработал**: в блоке t1r12
`invented_words` содержит «молотый», а `unread_layer_words` — «молодой». Но
сигнал не выделен: попал в «расхождение 21%» и в 49 строк орфографии.
**Решение.** Сопоставлять пары (слово vision не из слоя ↔ слово слоя не
прочитано) по редакционному расстоянию ≤ 2 → отдельная находка с высшим
приоритетом: «ВОЗМОЖНАЯ ПОДМЕНА СЛОВА: на макете «молодой», прочитано
«молотый» — vision мог исправить опечатку макета; проверьте макет». Код без
LLM, тесты на этом примере и на «hалейте/Налейте».
**Сделано 03.09.** `vision.word_substitutions`: пары «слово слоя не прочитано ↔ слово vision вне слоя» по расстоянию Левенштейна ≤ 2 (слова от 5 букв, каждое слово vision — один раз, ближайшая пара); гомоглифная пара помечается отдельно. Регион уходит в ручную проверку с причиной «ВОЗМОЖНАЯ ПОДМЕНА СЛОВА: на макете «молодой», прочитано «молотый»» — независимо от кривых (R-23). Попутно найден и исправлен дефект сверки: ядро для слов слоя считалось от каждой половины разрезанного кропа, и полоса слов на стыке половин (600 px на гёдза) в сверку не попадала — «молодой» лежал именно там. Теперь ядро — от объединения кропов. Проверено пересчётом по layout'ам: гёдза t1r12 — пара «молодой → молотый» найдена, слов слоя в сверке 353 → 455; манду t2r4 — гомоглиф «hалейте → Налейте». Находка идёт первой строкой в аспект 19.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-16 · Mandu: выдуманный штрихкод 4820140240955 — P2
**Данные.** В текстовом слое PDF один 13-значный код: 8805957025951
(контрольная сумма сходится). Код 4820140240955 (префикс 482 — Украина) в
слое отсутствует — выдуман моделью чтения в конце обрезанного блока t2r2.
Сторож выдумок его пропустил по двум причинам сразу: цифры не считаются
«словами» (`_INVENT_WORD` — только буквы), а строки «штрихкод: …» исключены
из проверки как описания графики. Промпт чтения требует «штрихкод: <цифры>»
и не даёт выхода, когда цифр не видно.
**Решение.** (1) В `barcode_check`: при наличии слоя сверять каждого
кандидата со слоем (цифры слоя нормализовать — «8 805957 025951»); код без
подтверждения слоем → «не подтверждён текстовым слоем — вероятная ошибка
чтения, ручная проверка». (2) Промпт чтения: если цифры не читаются —
«штрихкод: [неразборчиво]». (3) Число ≥ 8 цифр в vision-тексте, которого
нет в слое, — в сторож выдумок.
**Сделано 03.09.** (1) `barcode_check` сверяет каждый код с длинными числами текстового слоя (`layout.layer_digit_runs`, пишет vision/reguard): код не из слоя → «в текстовом слое такого кода НЕТ — вероятная ошибка чтения»; код слоя, не прочитанный vision, — отдельная находка; слой без цифр — честная оговорка; старый layout без поля — без оговорок. (2) Промпт чтения: цифры под штрихкодом переписывать только целиком, иначе «штрихкод: [неразборчиво]». (3) Числа ≥ 8 цифр vision-текста вне слоя — поле `invented_digits` региона, показываются в сверке шага 1. Проверено пересчётом манду: 4820140240955 помечен, в слое только 8805957025951; у выдуманного кода контрольная цифра, кстати, не сходится — но сверка со слоем не зависит от неё.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-17 · Mandu: 23 «неточности» орфографии — P2
Та же природа, что R-13 (обрезанный блок t2r2, R-01). Закрывается R-13.
**Сделано 03.09.** Закрыт группировкой R-13.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-18 · Крабовые палочки, аспект 20: объяснение читается как противоречие — P2
**Замечание.** Система пишет, что на макете есть «Содержит подсластитель…
слабительный эффект», и тут же просит проверить наличие надписи «содержит
подсластитель».
**Данные.** Голоса: ручная / ручная / нарушение. В объяснении смешаны два
разных требования: (1) 022 п. 4.4 ч. 12 — надпись про сахароспирты и
слабительное действие (на макете ЕСТЬ); (2) 029 ст. 9 п. 4 — для продукции
с добавленными сахаром И подсластителем — надпись «с сахаром и
подсластителем» (на макете НЕТ). Модель проверяла второе, но написала так,
что читается как первое. Дополнительно модель применила порог 029 («более
10% сахароспиртов») к надписи, которую 022 требует без порога — ослабила
вывод.
**Решение.** В check-текст аспекта 20 — явно: требования разделять по
надписям, для каждой: «норма → что требуется дословно → что есть на
макете → вывод»; при конфликте двух регламентов применяется более строгое
требование. Структурный ответ по пунктам (R-06, шаг B) решает это
системно.
**Сделано 03.09.** Check-текст аспекта 20 переписан: пять надписей пронумерованы и проверяются отдельно, для каждой — норма и адрес; для сахароспиртов явно: 022 ч.4.4 п.12 — без порога, порог «свыше 10%» есть только в 029, применяется более строгое; формат объяснения по каждой надписи «норма → что требуется дословно → что есть на макете → вывод», наличие одной надписи не закрывает другую, статус — по худшей.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-19 · Крабовые палочки, аспект 21: «источник белка» — P4
**Замечание.** Хотелось бы подробный разбор правомерности: система назвала
только условие «≥12% энергетической ценности», не показала расчёт, и не
упомянула второе условие — не менее 5% суточной потребности в белке на
100 г.
**Данные.** Прил. 5 к 022 (чанк tr_ts_022_2011:0105): «Источник» — белок
обеспечивает не менее 12% энергетической ценности **при условии, что**
количество белка на 100 г составляет не менее 5% суточной потребности.
Два условия одновременно — Сергей прав. Почему модель не показывает
расчёт: валидатор самодостаточности понижает статус за число с «%», которого
нет ни в цитатах, ни в фактах макета — расчётный процент туда не попадает
(исключение сделано только для арифметики КБЖУ).
**Решение.** Арифметика клеймов кодом (по образцу КБЖУ аспекта 6): по прил.
5 для белка/жира/сахара/энергии — расчёт условий с числами макета и
суточными нормами прил. 2; результат идёт в промпт и разрешён валидатору.
Крупная, но ценная для инструмента фича.
**Статус.** открыто — бэклог с высоким приоритетом, если останется время до
сдачи.

### R-20 · Крабовые палочки: «разнобой в записи Е-кодов» — P2
**Замечание.** Слово «разнобой» непонятно и не годится для формального
языка; неясно, что проверять и что делать.
**Данные.** Слово пришло из нашего промпта орфографии («разнобой в написании
терминов и Е-кодов») — модель его повторяет. По сути находка верная:
Е-коды написаны латинской «E» («E1414») — в русской маркировке индекс
пишется кириллической «Е». На креветках та же формулировка скрыла реальный
дефект: «варено-мороженые» и «варено-мороженные» на одном макете.
**Решение.** (1) Промпт: формула каждого пункта — «что не так → как должно
быть», без слова «разнобой». (2) Проверку латинской E в Е-кодах вынести в
код (регулярное выражение) с внятной формулировкой: «Е-код записан
латинской буквой: E1414 → Е1414». (3) Разные написания одного слова —
модель обязана привести обе формы: «одно слово в двух написаниях:
«мороженые» / «мороженные» — привести к одной норме».
**Сделано 03.09.** (1) Промпт орфографии: каждый пункт «что не так → как должно быть», обе формы дословно, слово «разнобой» запрещено. (2) Латинская E в Е-кодах — кодом (`verdict.ecode_alphabet_findings`): «Е-код записан латинской буквой E вместо кириллической Е: E1414 → Е1414 (регион …)», только русские/смешанные блоки, одна строка на регион; модели велено Е-коды не комментировать. (3) Разные написания одного слова — модель обязана привести обе формы и норму.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-21 · Креветки, аспект 7: «Изготовлено и упаковано:» не распознано как дата — P2
**Данные.** Надпись есть в блоке t1r5 (после пищевой ценности), вердикт:
«поле с словами «дата изготовления»/«дата производства» не обнаружено» —
модель не сочла «Изготовлено и упаковано:» аналогичным по смыслу (022
п. 4.7 ч. 3 такие слова допускает).
**Решение.** Доменное правило в check-текст аспекта 7 (решение владельца):
перечень допустимых синонимов поля даты — «изготовлено», «произведено»,
«дата выработки», «изготовлено и упаковано» — трактуются как поле даты
изготовления; отдельно проверять, есть ли рядом сама дата или указание места
нанесения.
**Сделано 03.09.** Check-текст аспекта 7: правило владельца — синонимы поля даты («изготовлено», «произведено», «дата производства», «дата выработки», «изготовлено и упаковано», «дата изготовления и упаковки») считаются полем даты изготовления; отдельно проверять место под дату или указание, где она нанесена.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-22 · Креветки, аспект 1: отказ от вывода из-за ненадёжных блоков — P2
**Замечание.** Система переложила решение на человека, ссылаясь на
ненадёжно прочитанные блоки. Должна дать однозначный вывод и пометить, что
блоки помечены ненадёжными, поэтому возможны неточности.
**Данные.** 8 из 13 блоков — «требует ручной проверки»: 6 — с причиной «слова
вне текстового слоя (возможная выдумка): Состав, Пищевая, ценность…»,
2 — «расхождение с текстовым слоем». Это слова,
которые на макете есть — ложные срабатывания сторожа (R-23). Модель
получила факты с пометкой «прочитан ненадёжно» и по правилу осторожности
ушла в ручную проверку.
**Решение.** (1) Правило промпта вердикта: ненадёжность блока — не основание
для отказа; вывод делается по имеющемуся тексту, в объяснении — отдельная
фраза «блоки … прочитаны ненадёжно, вывод требует сверки с макетом». (2)
Убрать причину — R-23.
**Сделано 03.09.** Правило 3 системного промпта: пометка «прочитан ненадёжно» — не основание для ручной проверки; вывод по тексту как есть, в объяснении отдельное предложение с перечнем таких регионов и требованием сверки; ручная — только если сам текст региона искажён. Пометка факта в промпте переименована: «сверить с макетом» вместо «требует ручной проверки» (старая формулировка подсказывала модели статус). Плюс сам источник ложных «ненадёжных» блоков закрыт R-23.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-23 · Креветки: ложные «выдумки» сторожа на PDF со смесью кривых и текста — P3
**Данные (найдено при разборе).** Покрытие слоя 97%, но в 6 блоках сторож
пометил «выдумками» реальные слова макета («Состав», «Пищевая»,
«ценность», название города изготовителя). Причина: в PDF часть текста живая (в слое), часть — в кривых;
сторож двусторонний и считает, что если слоя нет — это выдумка, хотя в
кривых зонах отсутствие слова в слое ничего не значит (оговорка есть в
докстринге, но проверка идёт по региону целиком, а внутри региона смесь).
**Решение.** Перед сторожем оценить плотность слоя внутри рамки региона:
если слов слоя в рамке заметно меньше, чем слов vision (например, < 50%),
регион считать «частично в кривых» — сторож выдумок не применять, статус
«прочитано (слой частичный)». Тесты на данных креветок.
**Сделано 03.09.** Разбор по данным: замер по трём макетам показал, что геометрический признак (доля «чернил» вне рамок слов слоя) не работает — на цветной упаковке гёдза t1r12 и манду t2r4 (там сторож ловил настоящие фабрикации) чернил вне слоя 42–60% из-за фона; вариант отвергнут. Работает признак по словам, двухуровневый: регион частично в кривых, если слов слоя в рамке < `layer_partial_ratio` (0.5) × слов vision (логотип SEOUL на манду: 4 к 16; изготовитель креветок: 2 к 23); страница «смесь кривых и текста», если доля проверяемых слов vision вне слоя ≥ `layer_partial_page_share` (0.2): креветки 0.41, гёдза 0.11, манду 0.03. В обоих случаях сторож выдумок статус не меняет, слова остаются в `invented_words` для сверки шага 1 (R-12), регион получает флаг `layer_partial`, страница — `text_layer_partial` + предупреждение на шаге 1. Сторожа вынесены в чистые функции `assess_region` / `finalize_layer_guards`; пересчёт по готовому layout'у без API — `--reguard`. Пересчёт по трём макетам: креветки 8 → 2 ручных (остались t2r9, L6 — расхождение со слоем, не выдумки); гёдза 3 → 1 (t1r12 — подмена R-15 и расхождение 18%); манду 5 → 3 (t1r8 логотип и L7 сняты, фабрикации t2r4 на месте). Тесты: 10 новых в test_vision.
**Статус.** сделано 03.09, ждёт подтверждения.

### R-24 · Качество чтения плавает между прогонами; нет автоповтора — P3
**Данные (найдено при разборе).** Один и тот же PDF Gyoza: покрытие слоя
98% (31.08) и 82% (02.09). Пользователь этого не видит, кроме числа
покрытия; система при низком покрытии ничего не делает.
**Решение.** Порог покрытия в конфиге (например 0,9): ниже — автоматически
перечитать регионы, где доля непрочитанных слов слоя высока (связано с
R-01), и показать на шаге 1 заметное предупреждение с списком непрочитанных
слов (R-12).
**Сделано частично 03.09.** Шаг 1 при покрытии ниже `coverage_warning_threshold` (0.9) показывает раскрывающийся список всех непрочитанных слов слоя (до 300) с подсказкой распознать заново или дописать блок; аспект 19 называет это пропуском распознавания (R-13). Автоповтор чтения регионов с высокой долей непрочитанных слов — открыт (нужны прогоны зрения).
**Статус.** предупреждение сделано 03.09; автоповтор открыт (P3, нужны прогоны).


## Приёмка 03.09 (перепрогоны после пакета P2 + распознавание)

Пять макетов перепрогнаны (проверки 17–21 в журнале). Замечания Сергея и
находки при разборе — R-25…R-40. Замечание по лепёшке (петля Мёбиуса не
распознана, аспект 15) — это R-07, без изменений (P3).

### R-25 · Находки орфографии меняются от прогона к прогону — P2
**Замечание.** На одном макете аспект 19 в разных прогонах называет разные
дефекты (то точки, то пробелы).
**Данные / причина.** Аспект 19 — один вызов CHEAP-модели без голосования и
без кэша (кэш вердиктов на него не распространяется); модель
недетерминирована, набор мелких пунктуационных находок «плавает».
Детерминированные находки (слой, Е-коды) стабильны.
**Сделано 04.09.** `spelling_votes: 3` (config → verdict): три вызова, в
отчёт — находки, повторившиеся не меньше чем в половине ответов; ключ —
регион + дефектный фрагмент (разные формулировки одного дефекта — одна
находка). Стоимость ≈ +$0,005 на макет. Тест `test_spelling_consensus_drops_unstable_remarks`.
**Проверка 04.09 (сравнение прогонов 17→22 креветки, 20→23 манду).**
Голосование работает: находки с одним голосом («лишний пробел в 18° C»,
«лишняя заглавная в Made In Korea», «Yellow C») ушли; «Варено-мороженные»
и гомоглиф стабильны. Но в консенсус попадает СТАБИЛЬНЫЙ шум: модель в
2 из 3 голосов комментирует английские и корейские строки («Name :»,
«mandu .», «cabb → cabbage», «조리예») вопреки запрету, и требует точку в
конце заголовков и подписей полей («Изготовлено и упаковано:» →
«…упаковано.», «Северные в панцире» → «…панцире.»). Промпт ужесточён
(только русский текст; точка в конце заголовка/подписи — не дефект;
обрезанные краем слова — не дефект) и добавлена страховка кодом
`filter_spelling_remarks`: фрагмент без кириллицы — отбрасывается; правка,
сводящаяся к знаку в конце строки, — отбрасывается. На данных прогонов 22/23
после фильтра остаются только русские находки по существу.
**Статус.** голосование подтверждено данными 04.09; фильтр кодом — ждёт прогона.

### R-26 · Креветки: текст по дуге не прочитан — P3
**Данные.** Фраза «вариант сервировки продукта» идёт полукругом; в блоках её
нет (в слое — есть ли, не проверено: могла быть в кривых).
**Сделано 04.09 (промпт).** В промпт чтения добавлено: текст по дуге, кругу,
вертикально и под углом тоже переписывать. Требует прогона зрения.
**Статус.** промпт дополнен 04.09; проверить прогоном.

### R-27 · «Регионы t1r5, t1r7 прочитаны ненадёжно» при надёжных блоках — P1
**Замечание.** Формулировка встречается в вердиктах, хотя блоки подтверждены.
**Данные / причина.** В отчёте креветок `vision.manual_regions` ПУСТ — все
регионы «прочитано», а аспект 10 пишет «Регионы t1r5, t1r7 прочитаны
ненадёжно». Это мой промах в R-22: пример в правиле 3 системного промпта
содержал конкретные id «t1r5, t1r7» — модель скопировала пример дословно.
**Сделано 04.09.** Пример обезличен («регион <id>»), добавлено «если
помеченных регионов нет — ничего об этом не пиши».
**Статус.** подтверждено 04.09.

### R-28 · Орфография: «X → Y» не говорит, какой вариант верный — P2
**Замечание.** «слово с ошибкой: «Варено-мороженные» → «Варено-мороженые»» —
непонятно, где ошибка, где норма.
**Сделано 04.09.** Ответ модели структурирован: поля `wrong` (как на макете)
и `correct` (единственный правильный вариант); строка отчёта: «что не так:
«как на макете» → правильно: «как должно быть» — «цитата» (регион)».
**Статус.** подтверждено 04.09.

### R-29 · «Следующий» откатывает после «Сохранить» — регрессия R-03 — P1
**Данные / причина.** R-03 запоминал обработанный клик подписью (x, y,
width, height). После «Сохранить» страница перестраивается (сообщение об
успехе, смена статуса блока → новая картинка), компонент кликов отдаёт тот
же старый клик, но с другими width/height на пиксели — подпись не
совпадает, клик считается новым, выбор откатывается на блок, по которому
кликали раньше. Компонент (streamlit-image-coordinates 0.2.1) присылает
`unix_time` клика — надёжный признак «новый/старый».
**Сделано 04.09.** Подпись клика — `unix_time` (запасной вариант —
нормированные координаты x/width, y/height).
**Статус.** подтверждено 04.09.

### R-30 · Сверка со слоем: раскрыть сразу, переименовать колонки — P2
**Сделано 04.09.** Блок раскрыт по умолчанию; колонки: «В текстовом слое PDF
есть, но при распознавании макета не прочитано» / «Прочитано при
распознавании, но в текстовом слое PDF нет».
**Статус.** подтверждено 04.09.

### R-31 · Гёдза, аспект 20: Е420 и порог 10% — P2 (вопрос владельцу)
**Замечание.** Система требует надпись о подсластителе для сорбита Е420 в
составе, «не указывая, что правило действует только при доле более 10%».
**Данные.** 022 ч.4.4 п.12 (в корпусе): «Маркировка пищевой продукции, в
состав которой входят подсластители-сахароспирты, непосредственно после
указания состава должна дополняться надписью…» — порога по доле в 022 НЕТ.
Порог «более 10% сахароспиртов» — в 029 ст.9 ч.4 п.1 д). Модель применила
022 буквально, как велит check-текст («при расхождении — более строгое»).
**Контраргумент (озвучен).** По букве 022 надпись нужна при любом
количестве сахароспиртов; практика рынка (Е420 как влагоудерживающий агент
в компоненте, доли процента) идёт по порогу 029. Решение — за владельцем:
(а) оставить строгое прочтение 022 → «возможное нарушение»; (б) применять
порог 029: доля сахароспиртов по макету неизвестна → «требует ручной
проверки: уточнить у поставщика долю сахароспиртов; надпись обязательна
при > 10%» (не нарушение). Вариант (б) — правка check-текста аспекта 20.
**Решение Сергея 04.09.** Вариант (а): предупреждать о надписи для
сахароспиртов всегда — и при доле менее 10%, и когда доля неизвестна.
Check-текст аспекта 20 уже так устроен (022 без порога, более строгое
правило), правок не требуется.
**Статус.** закрыто решением владельца 04.09 (логика оставлена).

### R-32 · Гёдза, аспект 2: лишние рассуждения — P2
**Данные.** «Мясной компонент указан как «свинина», а отдельной проблемы с
видом сырья по птице здесь нет» — модель пересказывает правило владельца
(вид мясного сырья, день 9), не относящееся к продукту.
**Сделано 04.09.** Правило 4 системного промпта: не перечислять проверки без
находок и правила, не относящиеся к продукту.
**Статус.** сделано 04.09, ждёт подтверждения.

### R-33 · Гёдза, аспект 12: ГМО — ручная проверка без причины — P2
**Замечание.** Система сомневается, не назвав компонентов, которые могут
быть ГМО; должна просить проверить конкретные компоненты и порог 0,9%.
**Сделано 04.09 (правило владельца в check-тексте аспекта 12).** Перечень
компонентов ГМО-риска (соя и производные, кукуруза, рапс, картофельный
крахмал, сахарная свёкла, рис, модифицированные крахмалы без сырья…). Нет
таких компонентов → «соответствует» (надпись не требуется). Есть → «требует
ручной проверки» с перечислением именно этих компонентов и вопросом
поставщику: не из ГМО или ≤ 0,9%. Для гёдза ожидание: соевый белок, соевый
соус, Е322 → ручная с этими тремя.
**Статус.** сделано 04.09, ждёт подтверждения.

### R-34 · Гёдза: «молодой перец» не найден орфографией — P2
**Данные / причина.** Прогон 03.09 сделан по КАНОНИЧЕСКОМУ layout'у
`gyoza.json` (31.08), где vision прочёл «молодой» верно, — пары подмены
(R-15) нет, потому что нет расхождения со слоем. Дефект «молодой перец»
может найти только корректор по смыслу; CHEAP-модель его не заметила.
**Сделано 04.09.** Промпт орфографии: явно про похожие, но не те слова
(«молодой перец» вместо «молотый», «мороженные» вместо «мороженые»).
Гарантии нет — это LLM; проверить прогоном.
**Статус.** сделано 04.09 (промпт); проверить прогоном.

### R-35 · Лепёшка: крупный вид блока обрезан — P2
**Причина.** Паддинг крупного вида — 3% стороны страницы; у макетов без
текстового слоя рамки обзора нечем прищёлкнуть к словам, погрешность 2–4%
съедала край блока.
**Сделано 04.09.** Запас = 4% стороны страницы + 10% размера блока
(config → ui.crop_pad_page_pct / crop_pad_block_pct).
**Статус.** подтверждено 04.09.

### R-36 · Манду: блок t1r2 прочитан плохо — P3
**Данные.** Лицевая панель с логотипом (кривые, стилизованные надписи):
обрывки бренда, «● 서울 ●», одиночные буквы, мусорные символы, «EAN», «знак EAC». Природа — R-01/R-07
(зрение на графике), лечится промптом чтения и повтором; требует прогонов.
**Статус.** открыто.

### R-37 · Манду: «знак EAC» приписан регионам без знака — P2
**Данные.** В канонич. `mandu.json` строка «знак EAC» в t1r2, t2r2 (×3), L11;
в UI-копии — в t1r15 (net_weight), t2r4, t3r3, L3 (крошечный служебный
регион 0,02×0,015). Причина — прайминг: промпт чтения давал пример «знак
EAC», модель дописывала его в любой регион с пиктограммой. Риск обратный
R-07: аспект 14 может «увидеть» знак там, где его нет.
**Сделано 04.09.** (1) Промпт чтения: без примера «знак EAC», знаки
описывать только реально видимые, формат «знак: <что изображено>». (2)
Check-текст аспекта 14: описания знаков — не текст макета; «знак EAC» в трёх
и более регионах или в служебных пометках → «требует ручной проверки:
убедиться глазами». Проверить прогоном зрения.
**Статус.** сделано 04.09; проверить прогоном.

### R-38 · Манду: код прочитан, аспект 18 пишет «не найден» — P1
**Данные / причина.** В блоке t2r2 текст «…82\n\n8 805957 025951»; регулярное
выражение цифр допускало любые пробельные символы, включая перенос строки,
и склеивало «82» с кодом в число из 15 цифр — ни 13, ни 12/14, поэтому «код
не найден». Новая сверка со слоем (R-16) на канонич. layout'е без поля
`layer_digit_runs` молчит корректно.
**Сделано 04.09.** Регулярное выражение — только пробелы внутри строки
(обычный, неразрывный, тонкий). Тест на «82\n\n8 805957 025951».
**Статус.** подтверждено 04.09.

### R-39 · Манду, аспект 21: «источник протеина» — одно условие из двух — P4
**Данные.** То же, что R-19: прил. 5 к 022 — два условия (≥ 12%
энергетической ценности от белка И ≥ 5% суточной потребности на 100 г);
модель проверила первое, второе не назвала, расчёт не показала (валидатор
понижает статус за расчётный %, не пришедший из цитат/фактов).
**Решение.** Арифметика клеймов кодом по образцу КБЖУ аспекта 6: из
разобранной пищевой ценности считать долю энергии от белка и % суточной
нормы (прил. 2 к 022), передавать в промпт аспекта 21 и разрешать валидатору.
Оценка — 1,5–2 часа с тестами. Делать сейчас или после сдачи — решение
владельца (P4 по плану 02.09).
**Статус.** бэклог P4 (решение Сергея 04.09) — вместе с R-19.

### R-40 · Два layout'а одного PDF; канонический не пересчитан — P2
**Данные.** Для гёдза и манду перепрогон 03.09 сделан по каноническим
`gyoza.json` / `mandu.json` (в списке шага 1 они рядом с копиями по имени
PDF, не отличить), а команда пересчёта `--reguard` шла по имени PDF и
канонические не тронула: отсюда «Регионы L1, L2, L3 прочитаны ненадёжно»
на гёдза и отсутствие сверки штрихкода со слоем на манду.
**Сделано 04.09.** (1) `python -m labelcheck.vision --reguard-all` —
пересчёт всех layout'ов, PDF ищется по `meta.source_pdf`. (2) В списке
макетов шага 1 подпись: имя файла · исходный PDF · дата файла.
**Статус.** сделано 04.09, ждёт подтверждения.

### R-41 · «Не найдены обязательные блоки: product_name» после R-10 — P2
**Данные (скриншот шага 1, гёдза, 04.09).** Предупреждение осталось, хотя
`product_name` убран из `required_kinds` (R-10). Причина: список `missing`
вычисляется при распознавании и хранится в layout'е; пересчёт `--reguard`
обновлял сторожа и покрытие, но не `missing` — старые layout'ы показывали
застывший список.
**Сделано 04.09.** `reguard_layout` пересчитывает `missing` по текущему
конфигу; тест. Применить: `python -m labelcheck.vision --reguard-all`.
**Статус.** сделано 04.09, ждёт подтверждения.

---

## История изменений журнала

- 2026-09-02 — журнал заведён; 24 записи по замечаниям приёмки 02.09
  (5 макетов: Roti, Gyoza, Mandu, крабовые палочки, креветки).
- 2026-09-02 — решения Сергея: R-05 автодетект убрать полностью; порядок
  работ — P1 (баги интерфейса) → дашборд → P2; R-09 агент — в бэклог.
- 2026-09-02 — пакет P1 сделан: R-03, R-04, R-05, R-06 (шаг A), R-08;
  R-11 закрыт через R-05. Тесты 229/229 (+9). Коммит `ee62a16`.
- 2026-09-02 — Сергей проверил все пять сценариев P1 на живом интерфейсе —
  подтверждено. Уточнён счёт блоков креветок (R-22/R-23): 8 из 13 ручных,
  из них 6 по «выдумкам».
- 2026-09-03 — дашборд мониторинга сдан (коммит `f24566c`). Решение Сергея:
  пакет «доверие к распознаванию» (R-23, R-15, R-12, R-16, R-13/R-17) делать
  вместе с P2, без прогонов зрения; R-01/R-24-автоповтор/R-07 — после
  (правки промптов зрения требуют перезамера по vision-эталону).
- 2026-09-03 — пакет сделан: R-23, R-15, R-12, R-16, R-13, R-17, R-20, R-21,
  R-14, R-22, R-18, R-10; R-24 — предупреждение. Тесты 259/259 (+14).
  Ждёт приёмки: пять перепрогонов (креветки, гёдза, манду, крабовые
  палочки, лепёшка) после `--reguard` существующих layout'ов.
- 2026-09-03 — приёмка пакета: пять перепрогонов (проверки 17–21). Новые
  замечания R-25…R-40; R-27 (мой промах в примере промпта R-22), R-29
  (регрессия R-03), R-38 (штрихкод через перенос строки) — P1.
- 2026-09-04 — сделаны R-25, R-27, R-28, R-29, R-30, R-32, R-33, R-35,
  R-38, R-40; промпты R-26, R-34, R-37 (проверить прогонами). Вопросы
  владельцу: R-31 (порог 10% для сахароспиртов), R-39 (= R-19, делать ли
  арифметику клеймов сейчас).
- 2026-09-04 — R-41 (застывший `missing` в старых layout'ах) — по скриншоту
  приёмки; рубрика закрыта: докер, README, демо-макет, тест гигиены.
- 2026-09-04 — Сергей подтвердил на живом интерфейсе R-27, R-28, R-29,
  R-30, R-35, R-38. Решения: R-31 — предупреждать всегда (логика оставлена);
  R-39 — P4. R-25 проверено по данным прогонов 17→22, 20→23: голосование
  работает, добавлен фильтр иноязычных находок и «точек в конце».
